сделать setup.py
только lib socket
без stdlib
разбираем строку argparse
клиент консольный - позволяет вводить команды и отправлять на сервер

TCP, UDP

s.listen(5) - очередь

t.join()
t.daemon = True - не рекомендуется

Condition, notify()